# Evaluation evidence map

This file is an index for a public code review. It deliberately separates demonstrated work from future integration.

| Tender item | Evidence |
|---|---|
| React / TypeScript | `src/App.tsx` |
| React Query | `useQuery` and `src/api.ts` |
| REST | typed `GET /parcels` boundary, with local demo fallback |
| GIS | Leaflet `MapContainer` + GeoJSON layer |
| Testing | React Testing Library test under `src/test` |
| Python | linked service `farm-registry-python-tools` |
| Mobile | linked service `farm-registry-mobile` |

No government credentials or personal data are included.
